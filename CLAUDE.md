# lab

## リポジトリ運用

- `main` に直接コミットしない。作業前に `<type>/<内容>` 形式でブランチを切る（例: `feat/oauth-login`）
- コミットメッセージ・PR・issueのタイトルはConventional Commits形式で、内容は日本語で書く
- GitHubの操作には `gh` CLIを使う
- `git add` / `git commit` / `git push` は、ユーザーが明示的に指示したときだけ実行する。
  ファイルの編集は指示の範囲で進めてよいが、ステージング・コミット・pushを自分の判断で始めない
- 対応方針が複数ある場合は、着手する前に必ず `AskUserQuestion` で選択肢を提示してユーザーに選ばせる。
  自分で決めて進めない
- PRの本文は常にそのブランチの最新のコードを説明している状態にする。
  PRがあるブランチにpushしたら、指示を待たずに `git diff main...HEAD` を読み直して本文の各セクションと照合し、
  食い違いがあれば `gh pr edit` で直す（手順は create-pr スキルの「更新」）。本文が古いまま次の作業に進まない

## IMPORTANT: 事実と推測を分ける

確認していないことを、確認済みであるかのように書かない。
成果物（PR・issue・コミット）だけでなく、ユーザーへの回答にも同じ基準を適用する。

- 事実を述べるときは根拠を併記する（`path:行番号` / 実行したコマンドと出力 / 取得したURL）
- 根拠を示せないものは「未確認」「推測」と明示する
- 確認する手段がない場合は「分からない」と答える。もっともらしい回答を組み立てない
- 記憶やパターンから答えず、一次情報にあたる:
  - このリポジトリのコード・設定 → ファイルを読む
  - コマンド・ツールの挙動 → 実行するか `--help` を読む
  - ライブラリ・API・外部サービスの仕様 → 公式ドキュメントを取得する
  - 過去の経緯 → `git log` / `gh` で確認する
- テストの成否、再現手順、バージョン、issue番号は、実際に確認した値だけを書く

## コードコメント

- コメントは日本語で書く
- 「何を」ではなく「なぜ」を書く。コードを読めば分かることは書かない
- 外部要因への対処（仕様上の制約、ライブラリの不具合、互換性のための回避策）には、根拠となるURLやissue番号を添える
- 確証のない内容を書くときは「推測」「未検証」と明示する
