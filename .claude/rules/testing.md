# テスト

- 入出力の振る舞いを assert する。内部呼び出しの回数や順序は固定しない
- 正常系だけで終えない。空・0・上限・null・権限なし・不正入力を含める
- 時刻・乱数・外部サービスに依存する値は固定値を注入する
- バグ修正は、まずその不具合で落ちるテストを書いてから直す
- 書いたテストは実装を意図的に壊して落ちることを確認し、元に戻す
- 「このテストを消したらどのバグが漏れるか」を一文で言えないテストは書かない
- 報告には実行したコマンドと出力を添える。未実行なら「未実行」と書く
