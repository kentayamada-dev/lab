version: "2"

sql:
  - engine: postgresql
    schema: schema.sql
    queries: queries
    database:
      uri: "${DB_URL}"
    gen:
      go:
        package: db
        out: gen/db
        sql_package: pgx/v5
    rules:
      - sqlc/db-prepare
